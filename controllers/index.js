const thoughtController = require('./thought-controllers');
const userController = require('./user-controllers');
const reactionController = require('/reaction-controllers');

module.exports = {
  thoughtController,
  userController,
  reactionController
};